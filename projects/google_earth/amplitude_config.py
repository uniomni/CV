# Configuration for appearance of amplitude data

vertical_scale = 300000
bar_width = 0.03
palette_file = 'amplitude_palette.cpt'

# This is the template for the legends that appear when
# bar's are clicked on. It must contain one numeric value
# corresponding to what the amplitude of this data set means
# This numeric value will be inserted when the data is read
# and should not be applied here.
legend_string = 'Wave amplitude = %.1f m'

# Units used in legends
units = 'metres'
